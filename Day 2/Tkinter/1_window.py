import tkinter as tk

if __name__ == "__main__": # To only execute when this program is run
    root = tk.Tk()
    root.title("Spotify") # Setting the title of the Window
    root.state("zoomed") # Setting the size of the Window to full-screen
    root.mainloop()
